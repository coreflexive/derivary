#predicate

>[!iparam]
>1. $A \in \textsf{Set}$

>[!param]
>2. $R \in A \sim A$

>[!assume]
>3. $\textsf{id}_A \subseteq R$

>[!observe]
>4. $(\forall a \mid a \in A : a \mathbin{R} a)$
>5. $(\forall a \mid a \in X : (\exists x \mid x \in X : a \mathbin{R} a)) \Leftarrow X \subseteq A$
