#predicate

>[!iparam]
>1. $A \in \textsf{Set}$
>2. $B \in \textsf{Set}$

>[!param]
>1. $R \in A \sim A$
>2. $S \in B \sim B$
>3. $f \in A \sim B$
>

>[!assume]
>1. [[Function]] $f$
>2. $(\forall b,'b \mid b \mathbin{S} b' : (\mathop{f}b) \mathbin{R} (\mathop{f} b'))$
