#combinator

>[!iparam]
>1. $A \in \textsf{Set}$
>2. $B \in \textsf{Set}$

>[!param]
>1. $R \in A \sim B$

>[!define]
>2. $\mathop{\textsf{Kernel}} R = R^{\circ} \cdot R$

>[!observe]
>3. $\textsf{Kernel} \in (B \sim B) \leftarrow (A \sim B)$ #needs-writeup
